# Netlify redirects file
# This ensures that all routes are handled by the React app

# Handle affiliate links specifically
/affiliate/* /index.html 200

# Handle all other SPA routes
/* /index.html 200